# Animal Prototype Design

This demonstrates the Prototype Design Pattern using an animal system. An AnimalRegistry stores prototype instances of Sheep, Cow, and Horse, and creates new objects by cloning them instead of instantiating from scratch. Each animal can be customized independently after cloning, showing how the pattern improves flexibility and reduces object creation cost.
